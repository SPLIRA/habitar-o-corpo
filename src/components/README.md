# components

Componentes de interface reutilizaveis e independentes de uma funcionalidade especifica.

Componentes desta pasta nao devem conhecer regras de negocio. Quando um componente pertencer a uma feature, mantenha-o dentro de `features/`.
